package com.firsov.bluetoothalarm.ui.theme

import androidx.compose.ui.graphics.Color

// --- DARK: Graphite & Carbon (Холодный серый) ---
val CarbonBlack = Color(0xFF121316)    // Фон всего экрана
val GraphiteGrey = Color(0xFF1E2025)   // Карточки и кнопки
val MetalSilver = Color(0xFF8E929B)    // Второстепенные элементы
val ColdSteel = Color(0xFFAFB5C1)      // Иконки и заголовки

// Акценты в темной теме (приглушенные)
val DarkSteelBlue = Color(0xFF5C7089)  // Для кнопки БЛОК
val DarkMutedGreen = Color(0xFF5B7365) // Для кнопки РАЗБЛОК

// --- LIGHT: Sand & Powder (Теплая пудра) ---
val PowderWhite = Color(0xFFFDF8F5)    // Фон (очень светлый беж)
val WarmBeige = Color(0xFFF3E9E2)      // Карточки и кнопки
val SandStone = Color(0xFFD7CCC8)      // Границы и акценты
val SoftCocoa = Color(0xFF5D5753)      // Текст

// Акценты в светлой теме
val MutedBlue = Color(0xFF78909C)      // Для кнопки БЛОК
val MutedSage = Color(0xFF81C784).copy(alpha = 0.8f) // Для кнопки РАЗБЛОК

val AlarmCrimson = Color(0xFFB71C1C)   // Тревога (сдержанный красный)
